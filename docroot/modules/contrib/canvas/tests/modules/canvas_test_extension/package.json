{
  "name": "@drupal-canvas/test-extension",
  "version": "0.0.0",
  "engines": {
    "node": ">=22.19.0 <23 || >=24.5.0"
  },
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup index.ts --format iife --clean",
    "dev": "tsup index.ts --format iife --watch",
    "type-check": "tsc --noEmit --pretty"
  },
  "dependencies": {
    "@drupal-canvas/extensions": "*"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "^5.8.3"
  }
}
